/** @jsx jsx */
import * as React from "react";
import { jsx, css } from "@emotion/core";

interface IProps {}

export const Centering: React.FC<IProps> = ({ children }) => {
  return (
    <div>
    </div>
  );
};
